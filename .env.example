# For ComfyUI service in docker-compose.yml
# Set to --cpu for non-NVIDIA systems or --nvidia-device 0 for NVIDIA systems
COMFYUI_EXTRA_ARGS=--cpu

# For Python backend (loaded by scripts/config.py)
# Update these paths to match your local machine's setup
GBS_CLI_PATH="/path/to/your/gbstudio"
EMULATOR_PATH="/path/to/your/emulator.app"
OLLAMA_API_URL="http://ollama:11434/api/generate"
COMFYUI_API_URL="http://comfyui:8188"
